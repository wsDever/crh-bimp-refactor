.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: rem(36px);
  
  .title.center {
  	display: flex;
  	align-items: center;
  	justify-content: center;
  	position: absolute;
  	left: rem(120px);
  	right: rem(120px);
  	top: 0;
  	bottom: 0;
  	pointer-events: none;
  }
}